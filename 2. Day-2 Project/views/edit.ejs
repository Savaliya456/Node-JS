<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #333;
            text-align: center;
        }
        form {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
        }
        input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 15px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin-top: 10px;
            cursor: pointer;
            border-radius: 4px;
        }
        input[type="submit"]:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

    <h1>Edit Page</h1>

    <form action="/updateData?id=<%=singleData.id%>" method="post">
        <input type="text" placeholder="Title" value="<%=singleData.title%>" name="title" required>
        <input type="text" placeholder="Author" value="<%=singleData.author%>" name="author" required>
        <input type="text" placeholder="Published Year" value="<%=singleData.year%>" name="year" required>
        <input type="text" placeholder="Pages" value="<%=singleData.pages%>" name="pages" required>
        <input type="text" placeholder="Price" value="<%=singleData.price%>" name="price" required>
        <input type="text" placeholder="Published Copies" value="<%=singleData.copies%>" name="copies" required>
        <input type="submit" value="Update">
    </form>
    
</body>
</html>
