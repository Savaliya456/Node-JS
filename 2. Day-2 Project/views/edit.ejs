<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #e9ecef;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #343a40;
            text-align: center;
            margin-bottom: 30px;
        }
        form {
            max-width: 600px;
            margin: 0 auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        input[type="text"], input[type="file"] {
            width: calc(100% - 22px);
            padding: 12px;
            margin-bottom: 15px;
            border: 1px solid #ced4da;
            border-radius: 8px;
            box-sizing: border-box;
        }
        input[type="file"] {
            padding: 6px;
            border: 1px solid #ced4da;
        }
        img {
            display: block;
            max-width: 150px;
            margin: 0 auto 20px;
            border-radius: 8px;
        }
        input[type="submit"] {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 15px 20px;
            font-size: 18px;
            cursor: pointer;
            border-radius: 8px;
            display: block;
            width: 100%;
            box-sizing: border-box;
        }
        input[type="submit"]:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>

    <h1>Edit Page</h1>

    <form action="/updateData?id=<%=singleData.id%>" method="post" enctype="multipart/form-data">
        <input type="file" name="image">
        <img src="<%=singleData.image%>" alt="Current Book Image">
        <input type="text" placeholder="Title" value="<%=singleData.title%>" name="title" required>
        <input type="text" placeholder="Author" value="<%=singleData.author%>" name="author" required>
        <input type="text" placeholder="Published Year" value="<%=singleData.year%>" name="year" required>
        <input type="text" placeholder="Pages" value="<%=singleData.pages%>" name="pages" required>
        <input type="text" placeholder="Price" value="<%=singleData.price%>" name="price" required>
        <input type="text" placeholder="Published Copies" value="<%=singleData.copies%>" name="copies" required>
        <input type="submit" value="Update">
    </form>
    
</body>
</html>
