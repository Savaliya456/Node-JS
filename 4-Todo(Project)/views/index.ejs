<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 2rem;
        }
        .container {
            max-width: 900px;
        }
        .form-control {
            border-radius: 50px;
        }
        .btn-custom {
          width: 100px;
            border-radius: 50px;
            background-color: #007bff;
            color: white;
        }
        .btn-custom:hover {
            background-color: #0056b3;
        }
        .table thead th {
            background-color: #007bff;
            color: white;
        }
        .table tbody tr:nth-child(odd) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">Task Manager</h1>

        <form action="/addtask" method="post" class="mb-4 d-flex">
            <input type="text" name="task" class="form-control me-2" placeholder="Add your task" required>
            <button type="submit" class="btn btn-custom">Add Task</button>
        </form>

        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Task</th>
                        <th scope="col" class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% allTask.forEach(task => { %>
                        <tr>
                            <th scope="row"><%= task.id %></th>
                            <td><%= task.task %></td>
                            <td class="text-center">
                                <a href="/deleteTask?id=<%= task.id %>" class="btn btn-danger btn-sm">Remove</a>
                                <a href="/edit?updateid=<%= task.id %>" class="btn btn-warning btn-sm text-white">Edit</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-bdF5ZDxZj2Tt9L8JHlK2YjqE3PvFJj5Fw6Qf79H6LxO0+w5uZ2K5Fz2YtnO4MXFf" crossorigin="anonymous"></script>
</body>
</html>
